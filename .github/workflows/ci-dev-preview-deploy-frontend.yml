# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: CI Dev Preview Deploy Frontend
"on": pull_request
jobs:
  dev_build_and_preview:
    env:
      GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
      GCLOUD_SERVICE_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE_DEV }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: ./package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run format
      - run: npm run build:common
      - run: npm run build:shared
      - run: touch $GOOGLE_APPLICATION_CREDENTIALS
      - run: echo $GCLOUD_SERVICE_KEY | base64 -d --ignore-garbage > $GOOGLE_APPLICATION_CREDENTIALS
      - run: npx firebase use dev
      - run: npm run build:lp
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE_DEV }}"
          projectId: proof-of-your-life-dev
          target: lp
      - run: npm run build:admin
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE_DEV }}"
          projectId: proof-of-your-life-dev
          target: admin
      - run: npm run build:web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE_DEV }}"
          projectId: proof-of-your-life-dev
          target: web
