# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: CD Prod Deploy Frontend
"on":
  workflow_dispatch:
permissions:
  actions: none
  checks: write
  contents: none
  deployments: none
  id-token: none
  issues: none
  discussions: none
  packages: none
  pages: none
  pull-requests: write
  repository-projects: none
  security-events: none
  statuses: none
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: ./package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run format
      - run: npm run build:common
      - run: npm run build:shared
      - run: npm run build:lp
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE }}"
          channelId: live
          projectId: proof-of-your-life
          target: lp
      - run: npm run build:admin
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE }}"
          channelId: live
          projectId: proof-of-your-life
          target: admin
      - run: npm run build:web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROOF_OF_YOUR_LIFE }}"
          channelId: live
          projectId: proof-of-your-life
          target: web
