# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: CD Dev Deploy All
"on":
  workflow_dispatch:
permissions:
  actions: none
  checks: write
  contents: none
  deployments: none
  id-token: none
  issues: none
  discussions: none
  packages: none
  pages: none
  pull-requests: write
  repository-projects: none
  security-events: none
  statuses: none
jobs:
  cd_dev_deploy_all:
    environment:
      name: dev
    env:
      GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
      GCLOUD_SERVICE_KEY: ${{ secrets.GCLOUD_SERVICE_KEY }}
      LP_ENVIRONMENT: ${{ secrets.LP_ENVIRONMENT }}
      ADMIN_ENVIRONMENT: ${{ secrets.ADMIN_ENVIRONMENT }}
      WEB_ENVIRONMENT: ${{ secrets.WEB_ENVIRONMENT }}
      FUNCTIONS_ENVIRONMENT: ${{ secrets.FUNCTIONS_ENVIRONMENT }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: |
            ./package-lock.json
            ./functions/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run format
      - run: npm run build:common
      - run: npm run build:shared
      - run: touch ./projects/lp/src/environments/environment.ts
      - run: echo $LP_ENVIRONMENT | base64 -d --ignore-garbage > ./projects/lp/src/environments/environment.ts
      - run: npm run build:lp
      - run: touch ./projects/admin/src/environments/environment.ts
      - run: echo $ADMIN_ENVIRONMENT | base64 -d --ignore-garbage > ./projects/admin/src/environments/environment.ts
      - run: npm run build:admin
      - run: touch ./projects/web/src/environments/environment.ts
      - run: echo $WEB_ENVIRONMENT | base64 -d --ignore-garbage > ./projects/web/src/environments/environment.ts
      - run: npm run build:web
      - run: npm ci
        working-directory: functions
      - run: npm run lint
        working-directory: functions
      - run: npm run format
        working-directory: functions
      - run: touch ./functions/.env
      - run: echo $FUNCTIONS_ENVIRONMENT | base64 -d --ignore-garbage > ./functions/.env
      - run: npm run build
        working-directory: functions
      - run: touch $GOOGLE_APPLICATION_CREDENTIALS
      - run: echo $GCLOUD_SERVICE_KEY | base64 -d --ignore-garbage > $GOOGLE_APPLICATION_CREDENTIALS
      - run: npx firebase use dev
      - run: npx firebase deploy
      - run: rm $GOOGLE_APPLICATION_CREDENTIALS
        if: ${{ always() }}
      - run: rm ./projects/lp/src/environments/environment.ts
        if: ${{ always() }}
      - run: rm ./projects/admin/src/environments/environment.ts
        if: ${{ always() }}
      - run: rm ./projects/web/src/environments/environment.ts
        if: ${{ always() }}
      - run: rm ./functions/.env
        if: ${{ always() }}
