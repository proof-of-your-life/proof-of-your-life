import { addAlias } from 'module-alias';

addAlias('@f', __dirname + '/');
addAlias('@common', __dirname + '/common');
addAlias('@utils', __dirname + '/utils');
addAlias('@v1', __dirname + '/v1');
addAlias('@v2', __dirname + '/v2');
